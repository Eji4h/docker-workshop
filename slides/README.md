# Docker Workshop — open-slide demo

10-page evaluation deck (jitrak theme, JetBrains Mono). Spec: `docs/superpowers/specs/2026-07-24-open-slide-demo-design.md`.

## Run

```bash
cd slides
npm install
npm run dev
```

Open the **demo** deck. `npm run check` asserts theme + 10 pages.

Work stays on branch `develop` until we decide to expand or merge.
